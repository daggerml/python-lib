## 1. Database And Native Surface

- [x] 1.1 Remove `call_with_resize` from the typed DB facade, Cython DB facade, stubs, and alias-only tests; verify repository search finds no remaining symbol and focused DB growth tests pass.
- [x] 1.2 Make `db.pyi` match the implemented typed transaction `get` and `put` signatures without raw flags or overloads; verify Pyright accepts the package and a stub-surface contract test rejects the removed arguments.
- [x] 1.3 Remove unreachable `DML_DB_ERR_HANDLE_INVALID`, `DML_DB_ERR_HANDLE_CLOSED`, `DML_DB_ERR_HANDLE_FORKED`, `DML_DB_ERR_TYPE_INVALID`, and `DML_DB_ERR_ENV_REOPENED` definitions and Cython return-code mappings, removing Python exception classes only when they have no direct current raise or caller; verify the native build and DB error tests pass, dead native names have zero maintained-tree matches, and directly raised `DmlDbInvalidTypeError` plus reachable transaction errors remain covered.
- [x] 1.4 Align native DB documentation and naming with the current environment-registry and transaction APIs, including the stale include guard and nonexistent reopen-handle documentation; verify documented symbols exist in first-party headers or source.
- [x] 1.5 Delete the dead `_core.types` aliases `MaybeRef`, `MaybeRefScalar`, `Collection`, `MaybeRefList`, `MaybeRefDict`, `MaybeRefCollection`, and `RefCollection`, all introduced together by `77b0eb01` with no later replacement family; verify `git grep` finds none in maintained code/docs, Ruff and Pyright pass, and the distinct used public `api.Collection` remains.

## 2. Strict Configuration And Local Formats

- [x] 2.1 Validate global and project JSON object structure and every flattened persisted key before precedence resolution, rejecting unknown, removed, or scope-inappropriate keys with the source path and key in the error; verify config contracts cover `remote.project`, `remote.remotes`, malformed nesting, and arbitrary unknown keys.
- [x] 2.2 Ensure config updates validate the complete existing JSON document and write only supported canonical keys while removed environment variables remain unmapped; verify update and precedence contract tests pass.
- [x] 2.3 Preserve the later shallow metadata shape introduced by `6c54144e` while deliberately renumbering its initial ID from `1` to exact non-boolean integer `0`; verify round-trip tests assert `{"version":0,"missing":[...]}` and reject `1`, booleans, extra fields, malformed refs, duplicates, and unsorted refs without a compatibility read.
- [x] 2.4 Remove TOML, project URI/owner/name, named-remote, retired hook, and obsolete environment assumptions from active initialization/configuration tests and fixtures; verify maintained source/tests/current docs/active specs contain no supported behavior for `.dml/config.toml`, `remote.project`, `remote.remotes`, `DML_DEFAULT_BRANCH`, `DML_PROJECT_NAME`, `DML_PROJECT_OWNER`, `DML_REMOTE_PROJECT`, `DML_REMOTE_NAME`, `DML_BRANCH`, `DML_REMOTE`, `DML_REMOTE_BUCKET`, `DML_REMOTE_PREFIX`, `DML_REPO`, `DML_DYNAMODB_TABLE`, `DML_REMOTE_CACHE`, or `DML_HOOK`.

## 3. Remote And Execution Storage

- [x] 3.1 Preserve the later one-project split-execution descriptor layout established by `56d84c60` while deliberately renumbering schema `2` to exact non-boolean integer `0`; verify descriptor tests accept only the complete version-0 object and reject schemas `1` and `2`, booleans, missing fields, and extra fields without fallback reads.
- [x] 3.2 Make descriptor initialization probe the entire endpoint root, not only `dml/`, before conditional creation; verify integration tests reject descriptor-less roots containing only `exec/`, CAS, refs, IO, or unrelated endpoint objects without mutation.
- [x] 3.3 Replace every singular `exec/edge/` producer, reader, traversal, and cleanup path with canonical `exec/edges/` and add exact-path assertions; verify cancellation, invalidation, and execution-call-edge contract tests pass with no singular-path fallback or dual write.
- [x] 3.4 Reuse exact execution metadata/state/driver validators in remote GC, reject partial, malformed, extra-field, extra-file, and unified records before CAS deletion, and trace roots only from validated split fields; verify focused GC tests cover each invalid shape and valid retained/collectable records.
- [x] 3.5 Delete the synthesized `meta` output alias as deletion-only narrowing, and remove obsolete active-record helper shapes, unified-record terminology, and broad malformed-record fallbacks that synthesize valid-looking graph or invalidation state; verify Git history does not claim a completed `meta` replacement and direct record, graph, invalidation, and raw-ref tests expose only canonical persisted `metadata` and strict split records.
- [x] 3.6 Add regression coverage proving cache pointers backed only by unsupported unified records are stale and unsupported execution objects are neither parsed nor specially preserved; verify runtime execution-record and remote GC contract tests pass.
- [x] 3.7 Replace remaining embedded/unified execution wording and behavior in admin, stale-lock, and child-registration paths: lock CAS targets `driver.json`, invalidation and lineage CAS target `state.json`, immutable identity remains in `metadata.json`, and fresh-launch rollback conditionally removes all owned split files; verify execution-admin, stale-lock, and call-edge contract tests assert exact files.

## 4. Executor And Adapter Protocol

- [x] 4.1 Rename executor cancellation parameters from `argv_ptr` to `argv_ref` across dispatch, base interfaces, built-in Script/Docker/Batch/SSH executors, nested forwarding, docs, and tests; verify repository search finds no `argv_ptr` and cancellation contract tests pass.
- [x] 4.2 Enforce exact operation response combinations at adapter/runtime boundaries, always reject retired `running`, require diagnostics for provider failures, and reject contradictory success/retry/error fields; verify parameterized invoke, cleanup, cancel, and SSH nested-response tests cover valid and invalid matrices.
- [x] 4.3 Tighten Docker, Batch, and SSH nested response validation so malformed status-less or diagnostic-less payloads cannot pass through while current provider failure codes remain supported; verify nested executor protocol and AWS resilience tests pass.
- [x] 4.4 Replace obsolete `running`/wire-`state` payloads in `test_ssh_executor_contract.py`, `test_adapter_registry_contract.py`, and `test_executor_registry_contract.py`, and remove stale `get_active`/`meta` mocks from `test_runtime_cancel_gates.py` and `tests/_core/helpers.py`; verify those files use current `retry`/`adapter_state`, exact split records, or purpose-specific sentinels while legitimate execution/backend `running` and executor-internal `state` remain.
- [x] 4.5 Replace every stale `extra_lines` reference and assertion with the supported `post_lines` option without adding an alias; verify script executor, packaged skill, agent guidance, and documentation contract tests pass and repository search finds no supported-use `extra_lines` examples.

## 5. CLI, Docs, Tests, And Specs

- [x] 5.1 Remove `aliases` extraction, alias collision iteration, alias registration iteration, and alias forwarding through `_GroupedChoicePseudoAction`, while retaining canonical `_name_parser_map` registration and grouped help; verify CLI tests pass and no command accepts `admin remote get-cache`, `admin remote invalidate-cache`, `admin gc`, or `admin remote gc` while `cache get`, `cache invalidate`, root `gc`, `gc --remote`, and `admin agent-skill` remain.
- [x] 5.2 Update `AGENTS.md`, `src/daggerml/SKILL.md`, `docs/sharp-bits-and-security.md`, `docs/use/reference/cli.md`, `docs/use/reference/python-authoring.md`, `docs/develop/architecture/dag-storage-and-types.md`, and `c/README.md` to remove the enumerated old option, command, config, storage, and native API descriptions; verify maintained documentation references only canonical JSON config, direct endpoints, version-0 formats, `post_lines`, `argv_ref`, split execution records, current CLI commands, and real native symbols.
- [x] 5.3 Collapse current codec documentation, requirement names, and tests to the later Dag-owned plugin contract established by `68d169f6`, removing Stage 1, Stage 2, and `CodecContext` migration language; verify codec API and plugin contract tests pass and active docs/spec text has no staged compatibility model.
- [x] 5.4 Replace completed test-migration requirements and the permanent ledger requirement with one steady-state taxonomy contract, delete the alias-only DB test in `test_types.py`, replace the removed-config preservation test in `test_config_resolution.py` with strict rejection, and remove `CONTRIBUTING.md` language permitting absent legacy folders; verify `pytest --collect-only` has no duplicate legacy suites and maintained test paths follow the later subsystem taxonomy established by `7d0b1ea3`.
- [x] 5.5 Apply the change deltas to all affected active OpenSpec capabilities and reconcile cross-capability contradictions about TOML, project identity, unified records, statuses, and version IDs; verify `openspec validate --all --strict` succeeds.

## 6. Coordinated Verification

- [x] 6.1 Re-run `git show`/`git log -S` for every row in the design replacement manifest and verify the implementation keeps the history-confirmed later surface; record any deletion-only cleanup or version-0 renumbering as such rather than claiming a later replacement commit.
- [x] 6.2 Use `git grep` across maintained first-party source, tests, current docs, and active specs for every exact old token/path in the design manifest and deletion inventories, including DB aliases/errors, all seven dead core aliases, all thirteen retired environment names, `argv_ptr`, supported-use `extra_lines`, adapter-wire `status = "running"`, removed CLI routes, `.dml/config.toml`, `remote.project`, `remote.remotes`, `refs/projects/`, `calls/from/`, `calls/to/`, singular `exec/edge/`, unified execution wording, and superseded schema IDs; verify every remaining match is vendored/archive history or an explicit rejection assertion, and verify whole obsolete files appear as deletions in `git diff --name-status`.
- [x] 6.3 Run focused DB/native, configuration, shallow-history, remote, execution-state, adapter, executor, CLI, skill, and documentation contract suites with `uv run --dev --all-extras pytest` selectors and verify all affected tests pass.
- [x] 6.4 Run `uv run --dev --all-extras ruff check .`, `uv run --dev --all-extras pyright`, `uv run --dev --all-extras pytest -m "not slow" .`, and the full `uv run --dev --all-extras pytest .`; verify all commands complete successfully.
- [x] 6.5 Review `git diff` for each old/new pair and verify code, stubs, C headers, tests, docs, packaged guidance, and active specs contain the fully specified later contract with no compatibility alias, fallback read, dual write, migration helper, stale fixture, or unrelated change.
